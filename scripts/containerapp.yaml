properties:
  configuration:
    ingress:
      external: true
      targetPort: 3000
      corsPolicy:
        allowedOrigins:
          - "https://www.walcron.com"
          - "https://yoonghan.github.io"
        allowedMethods:
          - "GET"
          - "POST"
          - "PUT"
          - "DELETE"
          - "OPTIONS"
        allowedHeaders:
          - "*"
        allowCredentials: true
  identity:
    type: SystemAssigned
  template:
    containers:
      - image: ghcr.io/yoonghan/az-walcron:latest
        name: walcron
        env:
          - name: COSMOS_ENDPOINT
            value: "https://walcron-db.documents.azure.com:443/"
          - name: COSMOS_DATABASE
            value: "TodoDatabase"
          - name: COSMOS_CONTAINER
            value: "Todos"
          - name: OTEL_SERVICE_NAME
            value: "walcron-backend"
          - name: OTEL_EXPORTER_OTLP_PROTOCOL
            value: "grpc"
          - name: OTEL_RESOURCE_ATTRIBUTES
            value: "service.name=walcron-backend,cloud.role_name=walcron-backend"
        resources:
          cpu: 0.5
          memory: 1.0Gi
        probes:
          - type: Liveness
            httpGet:
              path: "/healthz"
              port: 3000
            initialDelaySeconds: 5  # Increased for Rust warm-up
            periodSeconds: 10
            failureThreshold: 3
          - type: Readiness
            httpGet:
              path: "/healthz"
              port: 3000
            initialDelaySeconds: 5
            periodSeconds: 5
            successThreshold: 1
    scale:
      minReplicas: 0
      maxReplicas: 1