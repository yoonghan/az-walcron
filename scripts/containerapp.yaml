properties:
  configuration:
    ingress:
      external: true
      targetPort: 8080
      corsPolicy:
        allowedOrigins:
          - "https://www.walcron.com"
          - "https://yoonghan.github.io"
        allowedMethods:
          - "GET"
          - "POST"
          - "PUT"
          - "DELETE"
          - "OPTIONS"
        allowedHeaders:
          - "*"
        allowCredentials: true
  template:
    containers:
      - image: ghcr.io/yoonghan/az-walcron:latest
        name: walcron
        resources:
          cpu: 0.5
          memory: 1.0Gi
      - image: ghcr.io/yoonghan/az-walcron-frontend:latest
        name: walcron-frontend
        resources:
          cpu: 0.25
          memory: 0.5Gi
    scale:
      minReplicas: 0
      maxReplicas: 1
